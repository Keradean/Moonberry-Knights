using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class WeaponManager : Singelton<WeaponMangaer>
{
    [Header("Config")]
    [SerializeField] private Image weaponIcon; // UI element to display the weapon icon
    [SerializeField] private TextMeshProUGUI weaponManaTMP; // UI element to display the weapon name

    public void EquipWeapon(ItemWeapon weapon)
    {
        weaponIcon.sprite = weapon.Icon;
        weaponIcon.gameObject.SetActive(true);
        weaponManaTMP.text = weapon.Weapon.RequiredMana.ToString();
        weaponManaTMP.gameObject.SetActive(true);
    }
}
*/
